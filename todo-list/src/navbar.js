// Navbar module
const navbarModule = (components) => {
    const sidebar = document.createElement('div');
    navbar.id = 'navbar';

    // navbar.appendChild(ul);
    

    return navbar;
};

export { navbarModule };